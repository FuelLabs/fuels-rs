[package]
name = "wasm-tests"
version = "0.0.0"
edition = { workspace = true }
publish = false

[lib]
crate-type = ['cdylib']

[dependencies]
fuels = { workspace = true }
getrandom = { version = "0.2", features = ["js"] }

[dev-dependencies]
webassembly-test = "0.1"
