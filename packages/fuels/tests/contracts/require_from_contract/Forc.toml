[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "require_from_contract"

[dependencies]
library = { path = "../contract_require_abi/", package = "contract_require_abi" }
